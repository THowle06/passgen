[package]
name = "passgen"
version = "0.1.0"
edition = "2024"

[dependencies]
clipboard = { version = "0.5.0", optional = true }
clap = { version = "4.6.0", features = ["derive"] }
rand = "0.10.0"

[features]
clipboard-support = ["clipboard"]
